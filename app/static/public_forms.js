(() => { document.querySelectorAll("[data-public-form]").forEach(page => { const message=page.querySelector("[data-form-message]"); const counter=page.querySelector("[data-form-counter]"); if(!message||!counter)return; const update=()=>{counter.textContent=`${message.value.length} / ${message.maxLength||1000}`;message.style.height="auto";message.style.height=`${Math.max(140,message.scrollHeight)}px`;}; message.addEventListener("input",update); update(); }); })();
